# v0.1.1 Acceptance Record

Date: 2026-05-12
Local commit: release tag target `v0.1.1` (Pi page-switching validation run on `87980853c741`; this record commit only adds evidence/docs)
Tag: `v0.1.1`
Pi version/commit marker: `/api/version` returns `version=v0.1.1`; final release marker is refreshed by `sync-to-pi.sh`
Pi deploy method: `piboard/deployment/sync-to-pi.sh <pi-user@pi-host-or-ip>`

## Service

- active: yes
- enabled: yes
- ExecStart: `/usr/bin/python3 /home/<pi-user>/CC-UK-TR/piboard/main.py --portrait --display-rotate 90`
- SDL backend: `SDL_VIDEODRIVER=kmsdrm`, `SDL_AUDIODRIVER=dummy`

## API State

Sanitized `/api/state` summary after v0.1.1 safe acceptance reset:

- `current_app`: `uk_station`
- `uk_station.layout`: `single`
- `uk_station.slots`: `["mock"]`
- `mock.preset`: `cycle`
- `mock.cycle_presets`: `["overview", "train", "weather", "calendar"]`
- `train`: `station_crs=KGX`, `data_source=mock`, `api_key=""`
- `weather`: `location_mode=manual`, `city=London`, `api_key=""`, `units=metric`
- `calendar`: `ical_url=""`, `lookahead_days=3`
- `custom`: default safe example content
- `device_settings`: `brightness=1.0`, `orientation=portrait`

## Device

- hostname: `<pi-host>` (redacted)
- temp: `49.4-52.1 C` during final validation
- CPU: PiBoard `python3` sample `1.3%`
- memory: PiBoard RSS `121888 KB`; system memory `425 MB total`, `211 MB available`
- throttled: `0x0`

## Page Switching

- Overview: pass; state confirmed `layout=single`, `slots=["mock"]`, `mock.preset=overview`
- Train: pass; state confirmed `layout=single`, `slots=["train"]`
- Weather: pass; state confirmed `layout=single`, `slots=["weather"]`
- Calendar: pass; state confirmed `layout=single`, `slots=["calendar"]`
- Custom: pass; state confirmed `layout=single`, `slots=["custom"]`
- Final restored state: `single/mock + cycle`

## Data Truthfulness

- Overview: mixed summary; screenshot shows source statuses rather than pretending all data is live.
- Train: mock/demo in v0.1.1 safe state; board displays `DEMO` and `Rail demo data (mock)`.
- Weather: live Open-Meteo no-key path; screenshot footer is `Open-Meteo`.
- Calendar: mock/demo because `ical_url` is empty; screenshot footer is `Google Calendar (mock)`.
- Custom: user-authored content, not a live data source.

## Evidence

- screenshots:
  - `piboard/review_artifacts/pi-acceptance-v0.1.1/overview.png`
  - `piboard/review_artifacts/pi-acceptance-v0.1.1/train.png`
  - `piboard/review_artifacts/pi-acceptance-v0.1.1/weather.png`
  - `piboard/review_artifacts/pi-acceptance-v0.1.1/calendar.png`
  - `piboard/review_artifacts/pi-acceptance-v0.1.1/custom.png`
- command outputs:
  - `/opt/anaconda3/bin/python3 -m compileall -q piboard`: pass
  - `/opt/anaconda3/bin/python3 tests/smoke_v0_1_1.py`: pass
  - Open-Meteo no-key local check: `Open-Meteo`, `LONDON`, `11-12 C`, `OVERCAST`, `weather`
  - Huxley2 local check: HTTP 500 from public endpoint; not claimed as live verified
  - `systemctl is-active piboard.service`: `active`
  - `systemctl is-enabled piboard.service`: `enabled`
  - `/api/device-status`: `ok=true`, `is_pi=true`, `version=v0.1.1`, `throttled=0x0`
- logs if relevant:
  - direct `kmsdrm` service start succeeded after final sync
  - direct DRM framebuffer capture was not used because fb0 can expose tty/private network details; committed screenshots are Pi-rendered offscreen PNGs from current code

## Known Issues

- Huxley2 public endpoint returned HTTP 500 during local validation, so Train live is not a v0.1.1 verified claim.
- Calendar live path needs a safe public iCal fixture before it can be release-verified.
- Direct KMS framebuffer capture can expose the tty plane; use the offscreen Pi screenshot export helper for release evidence.

## Verdict

- v0.1.1 runs on Pi: yes
- confidence: high for local smoke, version marker, safe data boundaries, service health, and page switching
- remaining blocker: none for v0.1.1
