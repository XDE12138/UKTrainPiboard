# v0.1.1 Acceptance Record

Date: 2026-05-12
Local commit: pending final acceptance commit
Tag: v0.1.1
Pi version/commit marker: pending Pi deployment
Pi deploy method: `piboard/deployment/sync-to-pi.sh <pi-user@pi-host-or-ip>`

## Service

- active: pending Pi verification
- enabled: pending Pi verification
- ExecStart: pending Pi verification
- SDL backend: pending Pi verification

## API State

Pending Pi verification of `/api/state`.

## Device

- hostname: pending Pi verification
- temp: pending Pi verification
- CPU: pending Pi verification
- memory: pending Pi verification
- throttled: pending Pi verification

## Page Switching

- Overview: pending Pi verification
- Train: pending Pi verification
- Weather: pending Pi verification
- Calendar: pending Pi verification
- Custom: pending Pi verification

## Data Truthfulness

- Overview: mixed summary; source statuses must identify LIVE, MOCK, WAIT, or STALE.
- Train: default mock/demo; Huxley2 path implemented, but local release validation saw HTTP 500 from the public endpoint.
- Weather: Open-Meteo live path verified locally with no API key.
- Calendar: default mock/demo unless a safe iCal URL is configured; no private iCal URL is committed.
- Custom: user-authored content, not a live data source.

## Evidence

- screenshots: pending Pi verification
- command outputs:
  - `/opt/anaconda3/bin/python3 -m compileall -q piboard`: pass
  - `/opt/anaconda3/bin/python3 tests/smoke_v0_1_1.py`: pass
  - Open-Meteo no-key local check: `Open-Meteo`, `LONDON UPD 00:16`, `11°C`, `OVERCAST`, `weather`
  - Huxley2 local check: HTTP 500 from public endpoint; not claimed as live verified
- logs if relevant: pending Pi verification

## Known Issues

- Huxley2 public endpoint returned HTTP 500 during local validation.
- Calendar live path needs a safe public iCal fixture before it can be release-verified.

## Verdict

- v0.1.1 runs on Pi: pending
- confidence: local candidate only until Pi verification passes
- remaining blocker: Pi latest HEAD acceptance
